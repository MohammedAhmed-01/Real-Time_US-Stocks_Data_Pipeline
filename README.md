# Real-Time_US-Stocks_Data_Pipeline
A end-to-end streaming data pipeline that ingests historical stock data, processes it in real time, stores it in a data lake and analytical database, visualises it in Power BI, and serves an AI-powered RAG assistant — all containerised with Docker and orchestrated by Airflow.
